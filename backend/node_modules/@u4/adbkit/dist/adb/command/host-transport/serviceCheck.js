import Command from '../../command.js';
export default class ServiceCheckCommand extends Command {
    async execute(serviceName) {
        this.sendCommand(`exec:service check ${serviceName} 2>/dev/null`);
        await this.readOKAY();
        const data = await this.parser.readAll();
        return this._parse(data.toString());
    }
    _parse(value) {
        if (value.includes('not found'))
            return false;
        if (value.includes('found'))
            return true;
        return false;
    }
}
//# sourceMappingURL=serviceCheck.js.map