import Command from '../../command.js';
export default class ReverseCommand extends Command {
    async execute(remote, local) {
        await this._send(`reverse:forward:${remote};${local}`);
        await this.readOKAY();
        await this.readOKAY();
        return true;
    }
}
//# sourceMappingURL=reverse.js.map