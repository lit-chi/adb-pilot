import Connection from '../../connection.js';
import Command from '../../command.js';
import { Duplex } from 'node:stream';
import { DeviceClientOptions } from '../../../models/DeviceClientOptions.js';
export default class MonkeyCommand extends Command<Duplex> {
    private timeout;
    constructor(connection: Connection, timeout?: number, options?: Partial<DeviceClientOptions>);
    execute(port: number): Promise<Duplex>;
}
//# sourceMappingURL=monkey.d.ts.map