import Command from '../../command.js';
export default class DetachCommand extends Command {
    async execute(serial) {
        await this._send(`host-serial:${serial}:detach`);
        await this.readOKAY();
        return true;
    }
}
//# sourceMappingURL=detach.js.map